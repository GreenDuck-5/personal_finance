# Function for reading the active CSV and pulling any information for the specified time period by the user

# Find out what the current CSV is. Read the date column and check if the parts match what the user pt in. Example: User wants expenses from march (3) 2025 (Note, the day isn't provided so it's default will be nothing). In CSV, if month == CSV month and year == CSV year (same for day if given), add it to a list to be printed to the user